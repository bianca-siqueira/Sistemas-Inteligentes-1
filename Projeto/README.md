Para o dataset do projeto, usamos dois arquivos csv encontrados no kaggle:

https://www.kaggle.com/datasets/saurabhbagchi/books-dataset

Usamos "books.csv" e "ratings.csv" para manipular e chegar a tabela final utilizada
como dataset do projeto, selecionando apenas as colunas de interesse dos dois arquivos

BIBLIOTECAS NECESSÁRIAS:

pip install customtkinter

pip install pillow

pip install bcrypt

pip install pandas

pip install openpyxl

pip install scikit-surprise

pip install numpy

-> EXECUÇÃO

1: Rodar banco.py 

2: Rodar ini_banco.py

3: rodar python main.py
